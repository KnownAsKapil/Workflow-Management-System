import { useEffect, useState } from "react";
import type { TaskHistory } from "../types/history";
import { taskHistory } from "../features/tasks/taskHistory.api";


export default function TaskHistoryPage(taskId: number){
    useEffect(() => {
        
    },[])

    return(<>
    
        
    </>)
}